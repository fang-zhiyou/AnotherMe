import SwiftUI

struct ImportModelView: View {
    var body: some View {
        Form {
            
        }
        .navigationBarTitle("Import Model", displayMode: .inline)
    }
}

struct ImportModelView_Previews: PreviewProvider {
    static var previews: some View {
        ImportModelView()
    }
}
